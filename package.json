{
  "name": "racoder",
  "description": "Transcode internet radio streams to MP3 streams",
  "main": "./src/server.js",
  "scripts": {
    "start": "docker compose up",
    "build": "docker compose build",
    "deploy": "fly deploy",
    "test": "node --test --experimental-test-coverage --test-concurrency=4 src/**/*.test.js",
    "test:watch": "node --test --watch src/**/*.test.js"
  },
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  }
}
